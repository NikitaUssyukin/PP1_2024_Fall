#include <iostream>

using namespace std;

int main() {

    string s;

    getline(cin, s); // allows to input strings containing spaces, stops when encounters a new line

    cout << s << endl;

    return 0;
}