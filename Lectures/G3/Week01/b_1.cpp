#include <iostream>

using namespace std;

int main(){

    int a;  
    int b; 
    
    // at this point, a and b are undefined
    // they have no defined value
    // they can contain any "random" number

    a = 0;
    b = 0; 

    // now a and b have a defined value 
    
    cout << a + b << endl;

    return 0;
}