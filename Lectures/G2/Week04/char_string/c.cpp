#include <iostream>

using namespace std;

int main() {

    int a;

    cin >> a;

    // getting the char corressponding to the number in a
    // according to ASCII table
    cout << char(a) << endl;

    return 0;
}